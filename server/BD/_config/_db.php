<?php
    // Conexión a la base de datos en local
    // $host = 'localhost';
    // $dbname = 'COMUNICADOS';
    // $username = 'system';
    // $password = 'admin';

    // try {
    //  $conn = new PDO("oci:dbname=" . $host . ";charset=utf8", $username, $password);
    // } catch (PDOException $e) {
    //  die("Error: " . $e->getMessage());
    // }
